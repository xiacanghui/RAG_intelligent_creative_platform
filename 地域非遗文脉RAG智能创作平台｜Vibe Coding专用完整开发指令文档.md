# 地域非遗文脉RAG智能创作平台｜Vibe Coding专用完整开发指令文档

适配设备：16G DDR4 内存 \+ RTX4060 Laptop 8G 独显
离线主模型：qwen3\.6（GPU 加速）｜线上备用：讯飞星火 API
参赛属性：个人 OPC 单人参赛、AI \+ 文创及服务赛道
文档用途：全文一次性复制投喂 Vibe Coding，底层技术全部采用行业最优解自动实现；仅预留 3 处【人工重构区】空函数框架，不填充核心业务算法，交由我手动编写；开发全程自动生成大赛归档目录，适配申报材料核验；业务类选项主动弹窗提问，底层技术参数直接使用文档固定最优配置，无需询问。

## 一、全局基础项目信息

1. 项目全称：地域非遗文脉 RAG 智能创作平台

2. 产品形态：轻量化 B/S 单页 Web 应用，无客户端、无硬件依赖，本机 RTX4060 显卡 GPU 加速本地大模型推理，断网可完整运行全部功能

3. 双模型调度架构（强制落地，固定最优方案）

    - 离线主模型：阿里开源 qwen3\.6，Ollama 自动启用 RTX4060 GPU 量化推理，遵循 Apache2\.0 开源协议，作为 Demo 离线兜底核心

    - 线上备用模型：讯飞星火 V3 云端 API，网络正常时可一键切换，优化长文案、短视频脚本生成效果

    - 调度规则：程序默认加载本地 qwen3\.6；自动检测网络连通状态，支持用户手动强制切换模型源；完整记录每一次调用的模型来源、Token 消耗日志，仅本地存储不上传第三方

4. 向量存储：ChromaDB 本地持久化向量库，单机轻量化 RAG 行业最优选型

5. 开发硬性约束

    - 智能体完整实现所有底层工程、页面、接口、调度逻辑；仅 3 个核心业务算法预留【人工重构区】空函数 \+ 注释框架，不填充业务逻辑

    - 自动生成大赛专用`archive`归档全套目录，内置代码快照、修改日志存储模板，适配后续原创材料核验

    - 代码头部统一注释：基础工程代码由 Vibe Coding 智能体生成；非遗加权检索、Token 节流缓存、非遗风格校验三大核心业务算法由项目负责人人工独立重构开发

6. 合规强制约束

    - 全程不接入任何境外大模型、第三方云端知识库存储；所有非遗素材、对话缓存仅本地持久化

    - 页面底部展示所有开源组件名称、对应开源协议文本，适配申报表合规核查

## 二、完整产品功能需求（全部标准化自动实现）

### （一）前端单页国风 Web（最优轻量化方案：HTML5 \+ TailwindCSS v3 \+ 原生 JS，不引入 Vue/React 重型框架）

整体视觉：国风米白、赭石、青蓝低饱和配色，简约传统纹样装饰，适配文旅非遗产品调性

1. 顶部全局导航栏

    - 一键模型切换按钮（本地 qwen3\.6 / 讯飞星火 API）

    - 知识库管理入口、批量内容导出按钮、开源协议弹窗入口

2. 模块 1：非遗智能问答（RAG 核心模块）

    - 长文本输入框，自动限制输入汉字上限 800 字

    - 输出区分两块展示：匹配到的非遗原始史料来源 \+ 工艺分类标签、大模型完整回答文本

3. 模块 2：传统纹样生成模块

    - 下拉选择内置非遗品类：皮影、竹编、年画、剪纸、木雕、湘绣、苏绣

    - 输入自定义风格描述，自动输出标准化国风纹样 AI 绘图提示词

4. 模块 3：批量结构化文创内容生成
内置 4 套固定行业模板，一键生成完整结构化文本：
①景区线下导览解说文案 ②非遗科普短视频完整脚本 ③文创商品宣传介绍文案 ④游客国风朋友圈打卡文案

5. 模块 4：统一内容导出工具
单段文本一键复制；批量生成内容一键打包下载为本地 TXT 文件，自动规范命名文件

6. 模块 5：轻量化知识库简易后台

    - 支持批量上传本地 TXT 非遗文档，程序自动切片、向量化入库

    - 可视化展示当前知识库全部工艺分类条目，支持单条文档删除

7. 全局通用功能
页面实时展示当前使用模型、单次对话 Token 消耗统计；适配浏览器全屏演示，无多余广告、冗余弹窗

### （二）后端 Python FastAPI 轻量服务（行业最优选型）

1. 标准 RAG 全流程底层逻辑完整实现：文档加载→智能文本切片→文本向量化→向量库持久化存储→用户 Query 向量转换→基础相似度匹配检索

2. 双模型调度控制器完整封装，底层网络检测、切换逻辑全自动实现；仅业务加权排序逻辑预留【人工重构区】

3. 预留 3 处独立【人工重构区】，仅生成空函数与注释框架，底层调用链路完整封装，不填充业务算法：
① `rag_core.py`：非遗分类加权相似度排序算法插槽
② `utils/token_control.py`：Token 分层节流、重复问答本地缓存逻辑插槽
③ `utils/style_filter.py`：非遗风格强制校验过滤引擎插槽

4. 内置通用基础合规过滤工具：全局敏感词拦截、超长文本自动截断、违规宣传内容过滤，完整自动实现无需人工修改

5. 本地日志持久化工具：记录每一次问答输入、输出、调用模型、Token 消耗数值，日志仅本地存储，不上传任何第三方平台

### （三）知识库固定规范（自动落地）

1. 数据源限定：仅读取本地`knowledge_base`文件夹内公开无版权非遗 TXT 文本，不支持外部网络素材导入

2. 自动切片规则：单段文本 500 字以内，程序自动根据文档内容打上对应非遗工艺分类标签

3. 向量库持久化：程序重启自动加载本地向量存储，无需重复向量化文档

## 三、全栈固定最优技术栈（全部默认落地，无需询问调整）

### 前端技术栈

HTML5 \+ TailwindCSS v3 \+ 原生 JavaScript，轻量化无重型框架，降低部署、运行硬件门槛

### 后端技术栈

1. 服务框架：Python FastAPI（AI 项目通用轻量接口最优选择）

2. 本地大模型管理：Ollama，自动生成一键拉取、GPU 4bit 量化、启动脚本，适配 RTX4060 显卡自动启用显存加速

3. RAG 封装：LangChain 稳定社区版本，标准链式调用流程

4. 向量数据库：ChromaDB 本地持久向量存储

5. 环境配置安全方案：python\-dotenv 读取本地`.env`密钥文件，密钥不硬编码写入代码，安全规范最优解

### 模型推理固定最优参数

qwen3\.6 统一 4bit GGUF 量化，自动调用 RTX4060 Laptop 8G 独显显存推理；内存分配适配本机 16G 物理内存，平衡推理速度与硬件占用

## 四、强制标准项目目录结构（严格按此生成，禁止改动层级与文件名）

```Plain Text
地域非遗文脉RAG智能创作平台/
├─ main.py                  # FastAPI主服务启动入口（完整底层逻辑实现）
├─ ollama_run.py            # qwen3.6一键拉取、4bit量化、GPU加速启动脚本（最优参数写死）
├─ model_switch.py          # 双模型自动调度控制器（底层逻辑完整，预留人工业务插槽）
├─ rag_core.py              # RAG检索主逻辑，【人工重构区：非遗加权排序算法】
├─ vector_store/           # Chroma向量持久化存储目录
├─ static/                  # 前端页面、国风静态素材文件夹
│  └─ index.html            # 完整单页交互前端页面
├─ knowledge_base/          # 本地非遗素材存放文件夹，自动生成3份示范公开非遗TXT样例
├─ utils/
│  ├─ token_control.py      # Token节流管控工具，【人工重构区：缓存、截断优化逻辑】
│  ├─ style_filter.py       # 非遗风格校验过滤工具，【人工重构区：风格判定规则】
│  └─ content_check.py      # 通用内容合规过滤（完整自动实现，无需修改）
├─ requirements.txt         # 标准化完整依赖清单，支持离线本地pip安装
├─ .env.template            # 密钥配置模板（安全最优方案，本地手动填写密钥）
├─ archive/                 # 大赛申报归档固定文件夹（全自动生成全套模板）
│  ├─ prompts/
│  ├─ code_snapshots/
│  ├─ modify_log/
│  ├─ dev_timeline.md
│  └─ demo_records/
└─ README_AI开发说明.md     # 可直接复制粘贴至大赛申报表的项目开发说明文档模板
```

## 五、分阶段开发执行流程（底层技术全部自动实现，每阶段完成主动告知我；仅业务类选项弹窗提问确认）

### 阶段 1：项目基础骨架搭建

生成完整目录结构、requirements\.txt 依赖清单、\.env 配置模板、全部空白代码文件，清晰标注所有【人工重构区】注释；自动生成 3 份示范非遗知识库 TXT 样例；完成后主动告知我等待确认。

### 阶段 2：前端国风交互页面完整开发

全套 UI 视觉、按钮交互、前后端接口请求绑定、内容导出功能全部实现，采用文档规定国风最优视觉方案；仅页面风格、素材类业务调整主动提问，底层逻辑直接落地；完成后告知我确认视觉效果。

### 阶段 3：后端标准 RAG \+ 向量库完整底层逻辑

文档批量上传、自动切片、向量入库、基础相似度检索代码全部完整实现；仅加权排序业务模块预留空框架，不填充逻辑；完成后告知我。

### 阶段 4：Ollama 本地 qwen3\.6 全套对接脚本

内置适配 RTX4060 的 4bit 量化、GPU 加速最优参数，一键运行本地推理，封装标准化调用接口，所有底层参数固定无需调整；完成后告知我。

### 阶段 5：讯飞星火 API 对接 \+ 双模型自动调度系统

网络状态检测、双模型一键切换、调用日志、Token 消耗统计底层逻辑全部实现；Token 节流、风格过滤仅预留空白函数框架，标注人工修改区域；完成后告知我。

### 阶段 6：配套工具与大赛归档全套文档

内容导出工具、本地调用日志系统、开源协议注释、archive 归档全套模板、申报专用 README 文档全部自动生成；最后输出一段标准化完整文字，可直接粘贴至申报表「核心技术 / 算法简介」栏目。

## 六、开发提问区分规则（严格执行，禁止擅自默认业务方案）

### ✅ 底层技术类：全部直接使用文档固定最优解，**不向我提问**，自动落地

1. 模型量化精度、向量库分片策略、FastAPI 运行端口、LangChain 标准调用链路

2. Token 基础截断上限、本地缓存存储方式、\.env 密钥安全配置方案

3. 页面基础国风配色、离线依赖打包方案、日志本地存储格式

4. 开源组件版本、推理超时限制、接口并发限制、GPU 显存分配参数

### ⚠️ 业务需求类：遇到以下内容必须主动弹窗提问确认，不能自行默认方案

1. 示范非遗知识库 TXT 素材篇幅长短、覆盖的非遗工艺品类数量

2. 纹样生成模块是否额外对接第三方在线绘图 API，还是仅输出文字提示词

3. 前端是否增加 Token 消耗可视化统计面板

4. 向量库是否增加批量清空、批量备份导出功能

5. 前端是否增加大赛 Demo 演示指引弹窗

6. 文创内容模板是否需要新增额外行业文案模板

## 七、大赛申报配套强制开发规范（代码自动内置，无需额外调整）

1. 所有代码文件头部统一添加注释声明：本项目基础工程代码由 Vibe Coding 智能体生成；非遗分类加权检索算法、Token 分层节流缓存系统、非遗风格强制校验过滤引擎三大核心业务算法模块由项目负责人独立人工重构开发。

2. 每一段生成代码配套快照保存注释，适配`archive/code_snapshots`存档规范，方便后续申报材料核验原创开发过程。

3. 代码内完整标注全部开源组件名称、对应开源协议文本，直接适配申报表合规核查栏目填写。

4. 全程不引入任何境外大模型服务，所有知识库、推理流程、对话缓存均本地可控存储，无数据出境风险。

5. 全部开发完成后，输出一段完整标准化技术描述文本，可直接复制粘贴至大赛申报表「核心技术 / 算法简介」。

## 八、投喂智能体操作指引

1. 将本文档全文一次性完整发送至 Vibe Coding 智能体，完整读取全部约束与需求；

2. 明确告知智能体执行规则：底层技术全部采用文档规定最优解自动实现，仅业务相关选择主动弹窗提问确认；严格分 6 个阶段分步开发，每阶段开发完成主动向我汇报进度；

3. 所有标记【人工重构区】仅生成空函数与业务注释框架，不填充核心业务算法逻辑，完整留给我手动编写；

4. 无特殊业务调整需求时，禁止主动询问底层技术参数，直接使用文档固定配置落地开发。

> （注：部分内容可能由 AI 生成）
